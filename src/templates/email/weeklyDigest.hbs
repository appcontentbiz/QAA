<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Weekly Project Digest</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      background-color: #009688;
      color: white;
      padding: 20px;
      text-align: center;
      border-radius: 5px 5px 0 0;
    }
    .content {
      background-color: #f9f9f9;
      padding: 20px;
      border-radius: 0 0 5px 5px;
    }
    .stats-container {
      margin: 20px 0;
    }
    .stat-card {
      background-color: white;
      border: 1px solid #e0e0e0;
      border-radius: 5px;
      padding: 15px;
      margin: 10px 0;
    }
    .stat-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 10px;
    }
    .stat-title {
      color: #009688;
      font-weight: bold;
    }
    .stat-value {
      font-size: 24px;
      color: #333;
    }
    .trend {
      font-size: 14px;
      color: #666;
    }
    .trend-up {
      color: #4CAF50;
    }
    .trend-down {
      color: #f44336;
    }
    .activity-timeline {
      margin: 20px 0;
    }
    .activity-item {
      padding: 10px;
      border-left: 2px solid #009688;
      margin-left: 20px;
      position: relative;
    }
    .activity-item::before {
      content: '';
      width: 10px;
      height: 10px;
      background: #009688;
      border-radius: 50%;
      position: absolute;
      left: -6px;
      top: 15px;
    }
    .button {
      display: inline-block;
      padding: 12px 24px;
      background-color: #009688;
      color: white;
      text-decoration: none;
      border-radius: 5px;
      margin: 20px 0;
    }
    .footer {
      text-align: center;
      margin-top: 20px;
      font-size: 12px;
      color: #666;
    }
    .chart {
      width: 100%;
      height: 200px;
      background-color: white;
      border: 1px solid #e0e0e0;
      border-radius: 5px;
      margin: 15px 0;
      padding: 10px;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1>Your Weekly Project Digest</h1>
    <p>Week of {{stats.startDate}} - {{stats.endDate}}</p>
  </div>
  
  <div class="content">
    <p>Hi {{name}},</p>
    
    <p>Here's your weekly summary of project activities and achievements:</p>
    
    <div class="stats-container">
      <div class="stat-card">
        <div class="stat-header">
          <div class="stat-title">Active Projects</div>
          <div class="stat-value">{{stats.activeProjects}}</div>
        </div>
        <div class="trend {{#if stats.projectTrendUp}}trend-up{{else}}trend-down{{/if}}">
          {{stats.projectTrendPercentage}}% from last week
        </div>
      </div>
      
      <div class="stat-card">
        <div class="stat-header">
          <div class="stat-title">Components Created</div>
          <div class="stat-value">{{stats.newComponents}}</div>
        </div>
        <div class="trend {{#if stats.componentTrendUp}}trend-up{{else}}trend-down{{/if}}">
          {{stats.componentTrendPercentage}}% from last week
        </div>
      </div>
      
      <div class="stat-card">
        <div class="stat-header">
          <div class="stat-title">AI Interactions</div>
          <div class="stat-value">{{stats.aiInteractions}}</div>
        </div>
        <div class="trend {{#if stats.aiTrendUp}}trend-up{{else}}trend-down{{/if}}">
          {{stats.aiTrendPercentage}}% from last week
        </div>
      </div>
    </div>
    
    <div class="chart">
      <!-- Placeholder for activity chart -->
      <img src="{{stats.activityChartUrl}}" alt="Weekly Activity Chart" style="width: 100%;">
    </div>
    
    <h2>Recent Activity</h2>
    <div class="activity-timeline">
      {{#each stats.recentActivity}}
      <div class="activity-item">
        <strong>{{this.time}}</strong>
        <p>{{this.description}}</p>
      </div>
      {{/each}}
    </div>
    
    <h2>AI Insights</h2>
    <div class="stat-card">
      <p>{{stats.aiInsights}}</p>
    </div>
    
    <p style="text-align: center;">
      <a href="{{dashboardUrl}}" class="button">View Full Dashboard</a>
    </p>
  </div>
  
  <div class="footer">
    <p>This digest is customized based on your project activity.</p>
    <p>To adjust digest frequency or content, visit your notification settings.</p>
    <p>&copy; {{year}} Our Platform. All rights reserved.</p>
  </div>
</body>
</html>
